package arrayproblems;

import java.util.HashSet;
import java.util.Set;

public class Prime {
	
	public static boolean chechPrime(int prime) { 
		
		   int i=2;
		   while(i<prime) {
		   if(prime%i==0) {
			   return false;
		   }
		   i++;
		   }
		   return true;
	}
	
	
	public static int countVowels(String s) {
		int op=0;
		s=s.toUpperCase();
		for(int i=0;i<s.length();i++) {
			char ch=s.charAt(i);
			if(ch=='A' || ch=='E' || ch=='I'   || ch=='O'  ||ch=='U' ) {
				op++;
			}
			
		}
		
		return op;
	}
	
	public static int countMissingNumber(int[] ip) {
		int sum=0;
		int Actualsum=0;
		int max=0;
		int min=ip[0];
		for(int i=0;i<ip.length;i++) {
	           Actualsum+=ip[i];	
	           if(ip[i]>max) {
	           max=ip[i];
	           }
	           
		}
		System.out.println(max);
		
		for(int i=min;i<=max;i++) {
			sum+=i;
		}
		
		return sum-Actualsum;
		
	}
	public static int findduplicate(int[] ip) {
		    
		Set<Integer>set=new HashSet<>();
		
		for(int i=0;i<ip.length;i++) {
		if(set.add(ip[i])) {
		}else {
			return ip[i];
		}
		}
		return -1;
	   
	}
	
	public static int movezerostoend(int[] ip) {
	
		
	int min=0;
	int max=ip.length-1;
	int cnt=0;
	while(min<=max) {
		
		System.out.println("min"+min);
		System.out.println("max"+max);
		
		if(ip[min]==0 & ip[max]!=0) {
			int temp=ip[max];
			ip[max]=ip[min];
			ip[min]=temp;
			cnt++;
			
		}
		else if(ip[min]!=0) {
			min++;}
			else if(ip[max]==0) {
			max--;}
		}
	
	
	
	for(int i=0;i<ip.length;i++) {
		System.out.print(ip[i]);
	}
	
	
	int middle=ip.length-1
	
		return cnt;
	}
	
	
 public static void main(String[] args) {
	      System.out.println(chechPrime(7));
	      System.out.println(countVowels("sonata"));
	      System.out.println(countMissingNumber(new int[]{4, 5, 7, 8}));
	      System.out.println(findduplicate(new int[] {1, 3, 4, 3, 2}));
	     System.out.println(movezerostoend(new int[] {1,3,0,0, 4, 3, 2}));
	      
}
} 
