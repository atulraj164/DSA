package hashmap;
import java.util.HashMap;
import java.util.Map;
import java.util.ArrayList;
import java.util.Collections;





class hapsmap_lerning {
  /*    public static void main(String args[]) {
    	  int [] nums= {1,2,2,4,4,5};
    	  /*HashMap<Integer,Integer> maps=new HashMap<Integer,Integer>();
       for(int i:arr) {
    	   if(maps.containsKey(i)) {
    		   maps.replace(i, maps.get(i), maps.get(i)+1);
    		  // maps.put(i,maps.get(i)+1);
    	   }else {
    		   maps.put(i, 0);
    	   }
    	  
       }
     
       
 
      int ele=0;;
    	 for(Map.Entry<Integer,Integer> thismap:maps.entrySet()) {
    		if( thismap.getValue()==Collections.max(maps.values())) {
    			ele=thismap.getKey();
    		}
    	 }
    	 System.out.println(ele);
    	 
    	
    } 
    *//*int [] expectedNums=new int[nums.length];

    int k=0;
    int j=1;
    expectedNums[0]=nums[0];
    for(int i=1;i<nums.length;i++){
       
        if(nums[i]!=nums[i-1]){
            expectedNums[j]=nums[i];
            j++;
        }else{
             k++;
        }
    }
    System.out.println(k);
    for(int i=0;i<nums.length;i++){
    System.out.print(expectedNums[i]);
    }
}*/
      
    	  
}
